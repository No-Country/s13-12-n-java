{
  "name": "TypeScript & Java",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "customizations": {
    "vscode": {
      "extensions": [
        "esbenp.prettier-vscode",
        "vscjava.vscode-java-pack",
        "vmware.vscode-spring-boot"
      ]
    }
  }
  
  // "postCreateCommand": "bash -c 'if ! command -v java &> /dev/null; then sudo apt update && sudo apt install -y openjdk-17-jdk && sudo apt install -y maven && sudo apt install -y gradle; fi'"
}
 